<template>
    <DemoRow>
        <jk-pagination
            total="1000"
            pageSize="20"
            layout="@(['total', 'prev', 'pager', 'next', '->', 'sizes', 'jumper'])"
        />
    </DemoRow>
</template>